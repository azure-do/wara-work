# Google Apps Script のウェブアプリ URL（.../exec で終わるもの）
# 未設定のときは public/data/works.json を表示します
VITE_GAS_WEBAPP_URL=

# お問い合わせ API（省略時は開発時 /api/contact → contact-server へプロキシ）
# 本番で API を別 URL に出した場合: https://your-api.example.com/api/contact
# VITE_CONTACT_API_URL=
